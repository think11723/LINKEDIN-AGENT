# Core
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langgraph==0.2.45

python-dotenv==1.0.1
pydantic==2.9.2
rich==13.9.4

# Web API
fastapi>=0.104.0
uvicorn>=0.24.0
# Required for FastAPI File / Form / UploadFile (Railway install)
python-multipart>=0.0.12

# Search
duckduckgo-search==6.3.5

# Image
Pillow>=11.0.0
requests==2.32.3

# LinkedIn OAuth
requests-oauthlib>=1.3.0

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0
pytest-asyncio>=0.23.0

# Phase 7 — SaaS backend (auth + multi-user storage)
firebase-admin>=6.5.0
motor>=3.5.0
pymongo>=4.7.0
cryptography>=42.0.0
httpx>=0.27.0
mongomock-motor>=0.0.30

# Phase 8C — IANA timezone database (Windows ships without it; Linux/macOS already have it)
tzdata>=2024.1